# 2.5.1 Quản lý mức phạt (Fine Level Management)

## Thông tin chung
- **Actor:** Quản lý viên
- **Yêu cầu:** Đăng nhập vào hệ thống với vai trò quản lý viên
- **Mô tả:** Cho phép quản lý viên quản lý các mức phạt (thêm, sửa, xóa)

## Luồng xem danh sách mức phạt

```mermaid
flowchart TD
    Start([Bắt đầu]) --> CheckAuth{Đã đăng nhập<br/>với vai trò quản lý viên?}
    
    CheckAuth -->|Không| RedirectLogin[Chuyển hướng đến<br/>trang đăng nhập]
    CheckAuth -->|Có| NavigateFineManagement[Vào trang Quản lý mức phạt]
    
    RedirectLogin --> End([Kết thúc])
    
    NavigateFineManagement --> FetchFineLevels[Tải danh sách mức phạt<br/>từ database]
    FetchFineLevels --> DisplayTable[Hiển thị bảng mức phạt:<br/>- Tên mức phạt<br/>- Số tiền<br/>- Ngày phạt<br/>- Thao tác Sửa/Xóa]
    DisplayTable --> End
```

## Luồng thêm mức phạt mới

```mermaid
flowchart TD
    Start([Bắt đầu]) --> ViewFineLevels[Xem danh sách mức phạt]
    ViewFineLevels --> ClickAdd[Click nút Thêm mức phạt]
    ClickAdd --> ShowAddForm[Hiển thị form thêm mức phạt]
    ShowAddForm --> InputData[Nhập thông tin:<br/>- Tên mức phạt<br/>- Số tiền<br/>- Ngày phạt mặc định: ngày hiện tại]
    
    InputData --> Validate{Kiểm tra validation}
    
    Validate -->|Tên mức phạt để trống| ErrorNameEmpty[Hiển thị lỗi:<br/>Tên mức phạt không được để trống]
    Validate -->|Tên mức phạt > 25 ký tự| ErrorNameLength[Hiển thị lỗi:<br/>Tên mức phạt tối đa 25 ký tự]
    Validate -->|Số tiền <= 0| ErrorAmount[Hiển thị lỗi:<br/>Số tiền phải > 0]
    Validate -->|Số tiền không phải số| ErrorAmountType[Hiển thị lỗi:<br/>Số tiền phải là số]
    Validate -->|Ngày phạt không hợp lệ| ErrorDate[Hiển thị lỗi:<br/>Ngày phạt không hợp lệ]
    
    ErrorNameEmpty --> InputData
    ErrorNameLength --> InputData
    ErrorAmount --> InputData
    ErrorAmountType --> InputData
    ErrorDate --> InputData
    
    Validate -->|Tất cả hợp lệ| SaveFineLevel[Lưu mức phạt vào database]
    SaveFineLevel --> Success[Hiển thị thông báo:<br/>Thêm mức phạt thành công]
    Success --> RefreshList[Làm mới danh sách mức phạt]
    RefreshList --> End([Kết thúc])
```

## Luồng sửa mức phạt

```mermaid
flowchart TD
    Start([Bắt đầu]) --> ViewFineLevels[Xem danh sách mức phạt]
    ViewFineLevels --> ClickEdit[Click nút Sửa<br/>hoặc sửa trực tiếp trên bảng]
    ClickEdit --> ShowEditForm[Hiển thị form chỉnh sửa<br/>với dữ liệu hiện tại]
    ShowEditForm --> PrefillForm[Điền sẵn thông tin:<br/>- Tên mức phạt<br/>- Số tiền<br/>- Ngày phạt]
    
    PrefillForm --> InputChanges[Nhập/sửa thông tin]
    InputChanges --> Validate{Kiểm tra validation}
    
    Validate -->|Tên mức phạt để trống| ErrorNameEmpty[Hiển thị lỗi:<br/>Tên mức phạt không được để trống]
    Validate -->|Tên mức phạt > 25 ký tự| ErrorNameLength[Hiển thị lỗi:<br/>Tên mức phạt tối đa 25 ký tự]
    Validate -->|Số tiền <= 0| ErrorAmount[Hiển thị lỗi:<br/>Số tiền phải > 0]
    Validate -->|Số tiền không phải số| ErrorAmountType[Hiển thị lỗi:<br/>Số tiền phải là số]
    Validate -->|Ngày phạt không hợp lệ| ErrorDate[Hiển thị lỗi:<br/>Ngày phạt không hợp lệ]
    
    ErrorNameEmpty --> InputChanges
    ErrorNameLength --> InputChanges
    ErrorAmount --> InputChanges
    ErrorAmountType --> InputChanges
    ErrorDate --> InputChanges
    
    Validate -->|Tất cả hợp lệ| UpdateFineLevel[Cập nhật mức phạt vào database]
    UpdateFineLevel --> Success[Hiển thị thông báo:<br/>Sửa mức phạt thành công]
    Success --> RefreshList[Làm mới danh sách mức phạt]
    RefreshList --> End([Kết thúc])
```

## Luồng xóa mức phạt

```mermaid
flowchart TD
    Start([Bắt đầu]) --> ViewFineLevels[Xem danh sách mức phạt]
    ViewFineLevels --> ClickDelete[Click nút Xóa]
    ClickDelete --> CheckUsage{Mức phạt đang<br/>được sử dụng?}
    
    CheckUsage -->|Có| ErrorInUse[Hiển thị lỗi:<br/>Mức phạt đang được sử dụng.<br/>Không thể xóa]
    CheckUsage -->|Không| ShowConfirm[Hiển thị dialog xác nhận:<br/>Bạn có chắc chắn muốn xóa mức phạt này?]
    
    ErrorInUse --> End([Kết thúc])
    
    ShowConfirm --> UserConfirm{Người dùng xác nhận?}
    
    UserConfirm -->|Hủy| Cancel[Đóng dialog]
    UserConfirm -->|Xác nhận| DeleteFineLevel[Xóa mức phạt khỏi database]
    
    Cancel --> End
    
    DeleteFineLevel --> Success[Hiển thị thông báo:<br/>Xóa mức phạt thành công]
    Success --> RefreshList[Làm mới danh sách mức phạt]
    RefreshList --> End
```

## Luồng thay thế

### Luồng 1: Người dùng chưa đăng nhập
- Hệ thống chuyển hướng đến trang đăng nhập

### Luồng 2: Người dùng không phải quản lý viên
- Hệ thống chuyển hướng đến dashboard phù hợp với vai trò

### Luồng 3: Xóa mức phạt đang được sử dụng
- Hệ thống không cho phép xóa và hiển thị lỗi
- Mức phạt đang được sử dụng nếu có phiếu phạt nào đang sử dụng mức phạt này

### Luồng 4: Hủy thao tác
- Người dùng có thể hủy bất kỳ thao tác nào và quay lại danh sách

## Xử lý lỗi

```mermaid
flowchart TD
    Error([Lỗi xảy ra]) --> CheckErrorType{Loại lỗi?}
    
    CheckErrorType -->|Lỗi validation| ShowValidationError[Hiển thị lỗi validation<br/>cho từng trường]
    CheckErrorType -->|Mức phạt đang được sử dụng| ShowInUseError[Hiển thị lỗi:<br/>Mức phạt đang được sử dụng.<br/>Không thể xóa]
    CheckErrorType -->|Lỗi hệ thống| ShowSystemError[Hiển thị lỗi:<br/>Đã xảy ra lỗi hệ thống.<br/>Vui lòng thử lại sau]
    CheckErrorType -->|Lỗi database| ShowDBError[Hiển thị lỗi:<br/>Không thể lưu/xóa mức phạt.<br/>Vui lòng thử lại]
    
    ShowValidationError --> AllowRetry[Cho phép sửa và thử lại]
    ShowInUseError --> EndError([Kết thúc])
    ShowSystemError --> AllowRetry
    ShowDBError --> AllowRetry
    AllowRetry --> EndError
```

## Quy tắc validation

| Trường | Quy tắc |
|--------|---------|
| Tên mức phạt | Không được để trống, tối đa 25 ký tự |
| Số tiền | Phải > 0, kiểu số |
| Ngày phạt | Ngày hợp lệ, mặc định là ngày hiện tại |

## Quy tắc nghiệp vụ

### Xóa mức phạt:
- Chỉ được xóa khi **không có phiếu phạt nào** đang sử dụng mức phạt này
- Phải có xác nhận từ người dùng trước khi xóa

### Sửa mức phạt:
- Có thể sửa trực tiếp trên bảng (inline editing)
- Hoặc sửa qua form riêng
- Khi sửa, các phiếu phạt đã tạo trước đó vẫn giữ nguyên mức phạt cũ

### Ngày phạt:
- Mặc định là ngày hiện tại
- Có thể chỉnh sửa nếu cần

## Chi tiết kỹ thuật

### Kiểm tra mức phạt đang được sử dụng:
```sql
SELECT COUNT(*) 
FROM fines 
WHERE fine_level_id = ? 
  AND status != 'Đã thanh toán'
```

### Lưu mức phạt:
```sql
INSERT INTO fine_levels (
  name,
  amount,
  fine_date,
  created_at
) VALUES (?, ?, ?, NOW())
```

### Cập nhật mức phạt:
```sql
UPDATE fine_levels 
SET name = ?,
    amount = ?,
    fine_date = ?,
    updated_at = NOW()
WHERE id = ?
```

### Xóa mức phạt:
```sql
DELETE FROM fine_levels 
WHERE id = ? 
  AND NOT EXISTS (
    SELECT 1 FROM fines 
    WHERE fine_level_id = fine_levels.id
  )
```

## Thông tin hiển thị

### Bảng mức phạt:
- ID
- Tên mức phạt
- Số tiền
- Ngày phạt
- Thao tác (Sửa/Xóa)
- Có thể sắp xếp và tìm kiếm
- Hỗ trợ sửa trực tiếp trên bảng

### Form thêm/sửa mức phạt:
- Tên mức phạt (text input)
- Số tiền (number input)
- Ngày phạt (date picker, mặc định: ngày hiện tại)
- Nút "Lưu" và "Hủy"

