# 2.7.2 Báo Cáo Chi Tiết (Detailed Reports)

## Thông tin chung
- **Actor:** Quản lý viên, Nhân viên thư viện
- **Yêu cầu:** Đăng nhập vào hệ thống với vai trò quản lý viên hoặc nhân viên thư viện, có dashboard và dữ liệu đầy đủ
- **Mô tả:** Cho phép xem các báo cáo chi tiết với khả năng lọc theo thời gian và xuất CSV

## Luồng chính

```mermaid
flowchart TD
    Start([Bắt đầu]) --> CheckAuth{Đã đăng nhập<br/>với vai trò phù hợp?}
    
    CheckAuth -->|Không| RedirectLogin[Chuyển hướng đến<br/>trang đăng nhập]
    CheckAuth -->|Có| NavigateReports[Vào trang Báo cáo chi tiết]
    
    RedirectLogin --> End([Kết thúc])
    
    NavigateReports --> ShowReportTypes[Hiển thị các loại báo cáo:<br/>1. Báo cáo Sách<br/>2. Báo cáo Mượn Trả<br/>3. Báo cáo Phạt<br/>4. Báo cáo Sách Mất/Hư]
    
    ShowReportTypes --> SelectReportType[Chọn loại báo cáo]
    SelectReportType --> ShowTimeFilter[Hiển thị bộ lọc thời gian]
    ShowTimeFilter --> SelectTimeRange[Chọn khoảng thời gian:<br/>- Ngày<br/>- Tuần<br/>- Tháng<br/>- Quý<br/>- Năm]
    
    SelectTimeRange --> SelectDates[Chọn ngày bắt đầu<br/>và ngày kết thúc]
    SelectDates --> GenerateReport[Tạo báo cáo]
    GenerateReport --> FetchData[Tải dữ liệu từ database<br/>theo khoảng thời gian]
    FetchData --> ProcessData[Xử lý và tính toán dữ liệu]
    ProcessData --> DisplayReport[Hiển thị báo cáo:<br/>- Bảng dữ liệu<br/>- Biểu đồ nếu có]
    
    DisplayReport --> UserAction{Thao tác của người dùng?}
    
    UserAction -->|Xuất CSV| ExportCSV[Luồng xuất CSV]
    UserAction -->|Thay đổi bộ lọc| ChangeFilter[Thay đổi khoảng thời gian]
    UserAction -->|Chọn báo cáo khác| SelectReportType
    UserAction -->|Không thao tác| Stay[Giữ nguyên]
    
    ExportCSV --> DownloadFile[Tải file CSV]
    ChangeFilter --> GenerateReport
    DownloadFile --> End
    Stay --> End
```

## Luồng báo cáo Sách

```mermaid
flowchart TD
    Start([Bắt đầu]) --> SelectBookReport[Chọn Báo cáo Sách]
    SelectBookReport --> ApplyTimeFilter[Áp dụng bộ lọc thời gian]
    ApplyTimeFilter --> FetchBookData[Tải dữ liệu sách:<br/>- Tổng số sách<br/>- Tình trạng sách<br/>- Số lần mượn]
    
    FetchBookData --> CalculateStats[Tính toán thống kê:<br/>- Số sách theo thể loại<br/>- Số sách theo tình trạng<br/>- Số lần mượn theo sách]
    
    CalculateStats --> DisplayTable[Hiển thị bảng:<br/>- Tên sách<br/>- Thể loại<br/>- Tình trạng<br/>- Số lần mượn]
    
    DisplayTable --> DisplayChart[Hiển thị biểu đồ:<br/>- Biểu đồ cột: Số sách theo thể loại<br/>- Biểu đồ tròn: Tình trạng sách]
    
    DisplayChart --> End([Kết thúc])
```

## Luồng báo cáo Mượn Trả

```mermaid
flowchart TD
    Start([Bắt đầu]) --> SelectBorrowReport[Chọn Báo cáo Mượn Trả]
    SelectBorrowReport --> ApplyTimeFilter[Áp dụng bộ lọc thời gian]
    ApplyTimeFilter --> FetchBorrowData[Tải dữ liệu mượn trả:<br/>- Số lần mượn theo ngày/tháng/quý<br/>- Số lần trả theo ngày/tháng/quý]
    
    FetchBorrowData --> GroupByTime[Gom nhóm theo thời gian:<br/>- Theo ngày<br/>- Theo tháng<br/>- Theo quý]
    
    GroupByTime --> CalculateBorrowStats[Tính toán:<br/>- Tổng số lần mượn<br/>- Tổng số lần trả<br/>- Tỷ lệ trả đúng hạn]
    
    CalculateBorrowStats --> DisplayTable[Hiển thị bảng:<br/>- Ngày/Tháng/Quý<br/>- Số lần mượn<br/>- Số lần trả<br/>- Tỷ lệ trả đúng hạn]
    
    DisplayTable --> DisplayChart[Hiển thị biểu đồ:<br/>- Biểu đồ đường: Mượn/Trả theo thời gian<br/>- Biểu đồ cột: So sánh mượn và trả]
    
    DisplayChart --> End([Kết thúc])
```

## Luồng báo cáo Phạt

```mermaid
flowchart TD
    Start([Bắt đầu]) --> SelectFineReport[Chọn Báo cáo Phạt]
    SelectFineReport --> ApplyTimeFilter[Áp dụng bộ lọc thời gian]
    ApplyTimeFilter --> FetchFineData[Tải dữ liệu phạt:<br/>- Tổng doanh thu phạt<br/>- Số phiếu phạt theo loại<br/>- Danh sách người nợ ngoài hạn]
    
    FetchFineData --> CalculateFineStats[Tính toán:<br/>- Tổng doanh thu<br/>- Số phiếu phạt đã thanh toán<br/>- Số phiếu phạt chưa thanh toán<br/>- Số tiền phạt theo loại]
    
    CalculateFineStats --> DisplayTable[Hiển thị bảng:<br/>- Loại phạt<br/>- Số lượng<br/>- Tổng tiền]
    
    DisplayTable --> DisplayChart[Hiển thị biểu đồ:<br/>- Biểu đồ tròn: Phân bố loại phạt<br/>- Biểu đồ cột: Doanh thu theo thời gian]
    
    DisplayChart --> DisplayOverdueList[Hiển thị danh sách<br/>người nợ ngoài hạn:<br/>- Tên độc giả<br/>- Email<br/>- Số tiền nợ]
    
    DisplayOverdueList --> End([Kết thúc])
```

## Luồng báo cáo Sách Mất/Hư

```mermaid
flowchart TD
    Start([Bắt đầu]) --> SelectLostDamagedReport[Chọn Báo cáo Sách Mất/Hư]
    SelectLostDamagedReport --> ApplyTimeFilter[Áp dụng bộ lọc thời gian]
    ApplyTimeFilter --> FetchLostDamagedData[Tải dữ liệu sách mất/hư:<br/>- Danh sách sách mất<br/>- Danh sách sách hư hỏng]
    
    FetchLostDamagedData --> GroupByType[Gom nhóm theo loại:<br/>- Mất<br/>- Hư hỏng]
    
    GroupByType --> CalculateStats[Tính toán:<br/>- Số lượng sách mất<br/>- Số lượng sách hư hỏng<br/>- Tổng giá trị thiệt hại]
    
    CalculateStats --> DisplayTable[Hiển thị bảng:<br/>- Tên sách<br/>- Tác giả<br/>- Loại Mất/Hư<br/>- Ngày xảy ra<br/>- Giá trị thiệt hại]
    
    DisplayTable --> DisplayChart[Hiển thị biểu đồ:<br/>- Biểu đồ cột: Số sách mất/hư theo thời gian]
    
    DisplayChart --> End([Kết thúc])
```

## Luồng xuất CSV

```mermaid
flowchart TD
    Start([Bắt đầu]) --> ViewReport[Xem báo cáo]
    ViewReport --> ClickExport[Click nút Xuất CSV]
    ClickExport --> PrepareData[Chuẩn bị dữ liệu<br/>cho CSV]
    PrepareData --> FormatCSV[Định dạng dữ liệu<br/>theo chuẩn CSV]
    FormatCSV --> GenerateFile[Tạo file CSV]
    GenerateFile --> DownloadFile[Tải file CSV<br/>về máy]
    DownloadFile --> Success[Hiển thị thông báo:<br/>Xuất CSV thành công]
    Success --> End([Kết thúc])
```

## Luồng lọc theo khoảng thời gian

```mermaid
flowchart TD
    Start([Bắt đầu]) --> ViewReports[Xem trang báo cáo]
    ViewReports --> ClickTimeFilter[Click bộ lọc thời gian]
    ClickTimeFilter --> ShowOptions[Hiển thị tùy chọn:<br/>- Ngày<br/>- Tuần<br/>- Tháng<br/>- Quý<br/>- Năm<br/>- Tùy chỉnh]
    
    ShowOptions --> SelectOption[Chọn tùy chọn]
    SelectOption --> CheckCustom{Tùy chỉnh?}
    
    CheckCustom -->|Có| ShowDatePicker[Hiển thị date picker:<br/>- Ngày bắt đầu<br/>- Ngày kết thúc]
    CheckCustom -->|Không| CalculateRange[Tính toán khoảng thời gian<br/>tự động]
    
    ShowDatePicker --> SelectDates[Chọn ngày bắt đầu<br/>và ngày kết thúc]
    SelectDates --> ValidateDates{Kiểm tra ngày}
    
    ValidateDates -->|Ngày không hợp lệ| ErrorDates[Hiển thị lỗi:<br/>Ngày không hợp lệ]
    ValidateDates -->|Ngày hợp lệ| ApplyFilter[Áp dụng bộ lọc]
    
    ErrorDates --> SelectDates
    
    CalculateRange --> ApplyFilter
    ApplyFilter --> ReloadReport[Tải lại báo cáo<br/>với khoảng thời gian mới]
    ReloadReport --> End([Kết thúc])
```

## Luồng thay thế

### Luồng 1: Người dùng chưa đăng nhập
- Hệ thống chuyển hướng đến trang đăng nhập

### Luồng 2: Người dùng không phải nhân viên/quản lý
- Hệ thống chuyển hướng đến dashboard phù hợp với vai trò

### Luồng 3: Không có dữ liệu trong khoảng thời gian
- Hiển thị thông báo "Không có dữ liệu trong khoảng thời gian này"
- Hiển thị bảng trống hoặc biểu đồ rỗng

### Luồng 4: Lỗi xuất CSV
- Hiển thị lỗi và cho phép thử lại

## Xử lý lỗi

```mermaid
flowchart TD
    Error([Lỗi xảy ra]) --> CheckErrorType{Loại lỗi?}
    
    CheckErrorType -->|Ngày không hợp lệ| ShowDateError[Hiển thị lỗi:<br/>Ngày không hợp lệ]
    CheckErrorType -->|Lỗi tải dữ liệu| ShowLoadError[Hiển thị lỗi:<br/>Không thể tải dữ liệu báo cáo]
    CheckErrorType -->|Lỗi xuất CSV| ShowExportError[Hiển thị lỗi:<br/>Không thể xuất CSV]
    CheckErrorType -->|Lỗi hệ thống| ShowSystemError[Hiển thị lỗi:<br/>Đã xảy ra lỗi hệ thống.<br/>Vui lòng thử lại sau]
    
    ShowDateError --> AllowRetry[Cho phép chọn lại ngày]
    ShowLoadError --> AllowRetry
    ShowExportError --> AllowRetry
    ShowSystemError --> AllowRetry
    AllowRetry --> EndError([Kết thúc với lỗi])
```

## Quy tắc nghiệp vụ

### Các báo cáo có sẵn:

#### 1. Báo cáo Sách:
- Tổng số sách
- Tình trạng sách (Có sẵn, Đang mượn, Mất, Hư hỏng)
- Số lần mượn theo sách
- Phân bố sách theo thể loại

#### 2. Báo cáo Mượn Trả:
- Số lần mượn theo ngày/tháng/quý
- Số lần trả theo ngày/tháng/quý
- Tỷ lệ trả đúng hạn
- So sánh mượn và trả

#### 3. Báo cáo Phạt:
- Tổng doanh thu phạt
- Số phiếu phạt theo loại (Trả muộn, Hư hỏng, Mất)
- Số phiếu phạt đã thanh toán/chưa thanh toán
- Danh sách người nợ ngoài hạn

#### 4. Báo cáo Sách Mất/Hư:
- Danh sách sách cần thay thế
- Số lượng sách mất/hư hỏng
- Tổng giá trị thiệt hại

### Lọc theo khoảng thời gian:
- **Ngày**: Dữ liệu trong 1 ngày cụ thể
- **Tuần**: Dữ liệu trong 1 tuần (7 ngày)
- **Tháng**: Dữ liệu trong 1 tháng
- **Quý**: Dữ liệu trong 1 quý (3 tháng)
- **Năm**: Dữ liệu trong 1 năm
- **Tùy chỉnh**: Chọn ngày bắt đầu và ngày kết thúc

### Xuất CSV:
- Xuất tất cả dữ liệu hiển thị trong báo cáo
- File CSV có tên: `[Tên báo cáo]_[Ngày xuất].csv`
- Encoding: UTF-8 với BOM để hiển thị tiếng Việt đúng

## Chi tiết kỹ thuật

### Lấy dữ liệu báo cáo sách:
```sql
SELECT 
  b.title,
  c.name as category,
  b.status,
  COUNT(br.id) as borrow_count
FROM books b
LEFT JOIN categories c ON b.category_id = c.id
LEFT JOIN borrows br ON b.id = br.book_id
WHERE br.created_at BETWEEN ? AND ?
GROUP BY b.id, b.title, c.name, b.status
```

### Lấy dữ liệu báo cáo mượn trả:
```sql
SELECT 
  DATE(created_at) as date,
  COUNT(*) as borrow_count
FROM borrows
WHERE created_at BETWEEN ? AND ?
GROUP BY DATE(created_at)
ORDER BY date
```

### Lấy dữ liệu báo cáo phạt:
```sql
SELECT 
  fine_type,
  COUNT(*) as count,
  SUM(amount) as total_amount
FROM fines
WHERE created_at BETWEEN ? AND ?
GROUP BY fine_type
```

### Format CSV:
```javascript
const csvContent = [
  ['Header1', 'Header2', 'Header3'],
  ['Data1', 'Data2', 'Data3'],
  // ...
].map(row => row.join(',')).join('\n')
```

## Thông tin hiển thị

### Trang báo cáo:
- Dropdown chọn loại báo cáo
- Bộ lọc thời gian (dropdown + date picker)
- Bảng dữ liệu
- Biểu đồ (nếu có)
- Nút "Xuất CSV"

### Bảng dữ liệu:
- Có thể sắp xếp theo các cột
- Có thể tìm kiếm trong bảng
- Phân trang nếu dữ liệu nhiều

### Biểu đồ:
- Sử dụng thư viện Recharts
- Responsive, tự động điều chỉnh kích thước
- Có tooltip khi hover
- Có legend để phân biệt các series

