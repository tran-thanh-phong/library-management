# 2.5.2 Xem & Thanh Toán Khoản Phạt (Độc Giả) - View & Pay Fine (Reader)

## Thông tin chung
- **Actor:** Độc giả
- **Yêu cầu:** Đăng nhập vào hệ thống với vai trò độc giả, có phiếu phạt và mức phạt
- **Mô tả:** Cho phép độc giả xem danh sách khoản phạt và gửi yêu cầu thanh toán

## Luồng xem danh sách khoản phạt

```mermaid
flowchart TD
    Start([Bắt đầu]) --> CheckAuth{Đã đăng nhập<br/>với vai trò độc giả?}
    
    CheckAuth -->|Không| RedirectLogin[Chuyển hướng đến<br/>trang đăng nhập]
    CheckAuth -->|Có| NavigateFines[Vào trang Khoản phạt]
    
    RedirectLogin --> End([Kết thúc])
    
    NavigateFines --> FetchFines[Tải danh sách khoản phạt<br/>của độc giả]
    FetchFines --> FilterUnpaid[Lọc các khoản phạt<br/>chưa thanh toán]
    FilterUnpaid --> DisplayList[Hiển thị danh sách:<br/>- Nguyên nhân phạt<br/>- Số tiền<br/>- Ngày phạt<br/>- Trạng thái]
    
    DisplayList --> CheckEmpty{Có khoản phạt<br/>chưa thanh toán?}
    
    CheckEmpty -->|Không| ShowEmpty[Hiển thị:<br/>Bạn không có khoản phạt nào<br/>chưa thanh toán]
    CheckEmpty -->|Có| ShowPayButton[Hiển thị nút<br/>Thanh toán cho mỗi phiếu phạt]
    
    ShowEmpty --> End
    ShowPayButton --> End
```

## Luồng thanh toán khoản phạt

```mermaid
flowchart TD
    Start([Bắt đầu]) --> ViewFines[Xem danh sách khoản phạt]
    ViewFines --> SelectFine[Chọn phiếu phạt<br/>trạng thái: Chưa thanh toán]
    SelectFine --> CheckStatus{Trạng thái<br/>phiếu phạt?}
    
    CheckStatus -->|Đã thanh toán| ErrorPaid[Hiển thị lỗi:<br/>Phiếu phạt này đã được thanh toán]
    CheckStatus -->|Chờ xác nhận| ErrorPending[Hiển thị lỗi:<br/>Phiếu phạt này đang chờ<br/>nhân viên xác nhận]
    CheckStatus -->|Chưa thanh toán| ClickPay[Click nút Thanh toán]
    
    ErrorPaid --> End([Kết thúc])
    ErrorPending --> End
    
    ClickPay --> ShowPaymentInfo[Hiển thị thông tin thanh toán:<br/>- Số tiền phải trả<br/>- Phương thức: Chuyển khoản ngân hàng<br/>- Thông tin tài khoản ngân hàng]
    
    ShowPaymentInfo --> ShowInstructions[Hiển thị hướng dẫn:<br/>1. Chuyển khoản số tiền<br/>2. Nhấn Đã thanh toán<br/>3. Chờ nhân viên xác nhận]
    
    ShowInstructions --> UserAction{Người dùng<br/>đã chuyển khoản?}
    
    UserAction -->|Chưa| Wait[Chờ người dùng<br/>chuyển khoản]
    UserAction -->|Đã chuyển khoản| ClickPaid[Click nút Đã thanh toán]
    
    Wait --> UserAction
    
    ClickPaid --> ShowConfirmModal[Hiển thị modal xác nhận:<br/>Bạn đã chuyển khoản thành công?]
    ShowConfirmModal --> UserConfirm{Người dùng xác nhận?}
    
    UserConfirm -->|Hủy| Cancel[Đóng modal]
    UserConfirm -->|Xác nhận| UpdateStatus[Cập nhật trạng thái phiếu phạt:<br/>Chưa thanh toán → Chờ xác nhận]
    
    Cancel --> End
    
    UpdateStatus --> SaveDB[Lưu vào database]
    SaveDB --> Success[Hiển thị thông báo:<br/>Yêu cầu thanh toán đã được gửi.<br/>Vui lòng chờ nhân viên xác nhận]
    Success --> RefreshList[Làm mới danh sách]
    RefreshList --> End
```

## Luồng xem chi tiết phiếu phạt

```mermaid
flowchart TD
    Start([Bắt đầu]) --> ViewFines[Xem danh sách khoản phạt]
    ViewFines --> ClickDetail[Click vào phiếu phạt<br/>để xem chi tiết]
    ClickDetail --> LoadDetail[Tải thông tin chi tiết:<br/>- Nguyên nhân phạt<br/>- Số tiền<br/>- Ngày phạt<br/>- Trạng thái<br/>- Ghi chú nếu có]
    LoadDetail --> DisplayDetail[Hiển thị chi tiết phiếu phạt]
    DisplayDetail --> End([Kết thúc])
```

## Luồng lọc theo trạng thái

```mermaid
flowchart TD
    Start([Bắt đầu]) --> ViewFines[Xem danh sách khoản phạt]
    ViewFines --> ClickFilter[Click dropdown<br/>Lọc theo trạng thái]
    ClickFilter --> ShowOptions[Hiển thị tùy chọn:<br/>- Tất cả<br/>- Chưa thanh toán<br/>- Chờ xác nhận<br/>- Đã thanh toán<br/>- Từ chối]
    ShowOptions --> SelectStatus[Chọn trạng thái]
    SelectStatus --> ApplyFilter[Áp dụng bộ lọc]
    ApplyFilter --> ReloadList[Tải lại danh sách<br/>theo trạng thái đã chọn]
    ReloadList --> DisplayFiltered[Hiển thị danh sách<br/>đã lọc]
    DisplayFiltered --> RemoveFilter{Click xóa bộ lọc?}
    
    RemoveFilter -->|Có| ResetList[Hiển thị lại<br/>tất cả khoản phạt]
    RemoveFilter -->|Không| End([Kết thúc])
    ResetList --> End
```

## Luồng thay thế

### Luồng 1: Người dùng chưa đăng nhập
- Hệ thống chuyển hướng đến trang đăng nhập

### Luồng 2: Không có khoản phạt
- Hiển thị thông báo "Bạn không có khoản phạt nào"

### Luồng 3: Phiếu phạt đã được thanh toán
- Ẩn hoặc vô hiệu hóa nút "Thanh toán"
- Hiển thị trạng thái "Đã thanh toán"

### Luồng 4: Phiếu phạt đang chờ xác nhận
- Hiển thị trạng thái "Chờ xác nhận"
- Ẩn hoặc vô hiệu hóa nút "Thanh toán"
- Hiển thị thông báo "Đang chờ nhân viên xác nhận"

### Luồng 5: Hủy thao tác
- Người dùng có thể hủy bất kỳ thao tác nào và quay lại danh sách

## Xử lý lỗi

```mermaid
flowchart TD
    Error([Lỗi xảy ra]) --> CheckErrorType{Loại lỗi?}
    
    CheckErrorType -->|Phiếu phạt đã thanh toán| ShowPaidError[Hiển thị lỗi:<br/>Phiếu phạt này đã được thanh toán]
    CheckErrorType -->|Phiếu phạt đang chờ xác nhận| ShowPendingError[Hiển thị lỗi:<br/>Phiếu phạt này đang chờ xác nhận]
    CheckErrorType -->|Lỗi tải dữ liệu| ShowLoadError[Hiển thị lỗi:<br/>Không thể tải danh sách khoản phạt]
    CheckErrorType -->|Lỗi hệ thống| ShowSystemError[Hiển thị lỗi:<br/>Đã xảy ra lỗi hệ thống.<br/>Vui lòng thử lại sau]
    
    ShowPaidError --> EndError([Kết thúc])
    ShowPendingError --> EndError
    ShowLoadError --> AllowRetry[Hiển thị nút Thử lại]
    ShowSystemError --> AllowRetry
    AllowRetry --> ClickRetry{Click Thử lại?}
    ClickRetry -->|Có| Reload[Tải lại dữ liệu]
    ClickRetry -->|Không| EndError
    Reload --> EndError
```

## Quy tắc nghiệp vụ

### Thanh toán khoản phạt:
1. Chỉ có thể thanh toán phiếu phạt ở trạng thái **"Chưa thanh toán"**
2. Sau khi click "Đã thanh toán", trạng thái chuyển thành **"Chờ xác nhận"**
3. Độc giả cần **chuyển khoản qua ngân hàng** trước khi click "Đã thanh toán"
4. Sau khi gửi yêu cầu, phải chờ nhân viên xác nhận (xem 2.5.3)

### Trạng thái phiếu phạt:
- **"Chưa thanh toán"**: Chưa thanh toán, có thể click "Thanh toán"
- **"Chờ xác nhận"**: Đã gửi yêu cầu, chờ nhân viên xác nhận
- **"Đã thanh toán"**: Đã được nhân viên xác nhận
- **"Từ chối"**: Bị nhân viên từ chối (số tiền không phù hợp)

## Chi tiết kỹ thuật

### Cập nhật trạng thái phiếu phạt:
```sql
UPDATE fines 
SET status = 'Chờ xác nhận',
    payment_requested_at = NOW()
WHERE id = ? 
  AND reader_id = ?
  AND status = 'Chưa thanh toán'
```

### Lấy danh sách khoản phạt:
```sql
SELECT 
  f.id,
  f.fine_type,
  f.amount,
  f.fine_date,
  f.status,
  f.notes,
  fl.name as fine_level_name
FROM fines f
LEFT JOIN fine_levels fl ON f.fine_level_id = fl.id
WHERE f.reader_id = ?
ORDER BY f.fine_date DESC
```

## Thông tin hiển thị

### Danh sách khoản phạt:
- Nguyên nhân phạt (Trả muộn, Hư hỏng, Mất sách)
- Số tiền
- Ngày phạt
- Trạng thái
- Nút "Thanh toán" (chỉ hiển thị khi trạng thái = "Chưa thanh toán")
- Nút "Xem chi tiết"

### Thông tin thanh toán:
- Số tiền phải trả
- Phương thức thanh toán: Chuyển khoản ngân hàng
- Thông tin tài khoản ngân hàng (số tài khoản, tên chủ tài khoản, ngân hàng)
- Hướng dẫn thanh toán

### Chi tiết phiếu phạt:
- Nguyên nhân phạt
- Mức phạt
- Số tiền
- Ngày phạt
- Trạng thái
- Ghi chú (nếu có)
- Ngày yêu cầu thanh toán (nếu đã gửi)
- Ngày xác nhận thanh toán (nếu đã xác nhận)

