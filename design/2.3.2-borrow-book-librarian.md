# 2.3.2 Mượn Sách (Nhân Viên Thư Viện) - Borrow Book (Librarian)

## Thông tin chung
- **Actor:** Nhân viên thư viện
- **Yêu cầu:** Đăng nhập vào hệ thống với vai trò nhân viên thư viện
- **Mô tả:** Cho phép nhân viên thư viện xem và xử lý các yêu cầu mượn sách (xác nhận hoặc từ chối)

## Luồng chính

```mermaid
flowchart TD
    Start([Bắt đầu]) --> CheckAuth{Đã đăng nhập<br/>với vai trò nhân viên?}
    
    CheckAuth -->|Không| RedirectLogin[Chuyển hướng đến<br/>trang đăng nhập]
    CheckAuth -->|Có| ViewPendingList[Xem danh sách đơn mượn<br/>chờ xác nhận]
    
    RedirectLogin --> End([Kết thúc])
    
    ViewPendingList --> FetchPending[Tải danh sách đơn mượn<br/>trạng thái: Chờ xác nhận]
    FetchPending --> DisplayList[Hiển thị danh sách:<br/>- Tên độc giả<br/>- Tên sách<br/>- Ngày yêu cầu<br/>- Thời hạn mượn<br/>- Ngày hết hạn dự kiến]
    
    DisplayList --> CheckEmpty{Có đơn mượn<br/>chờ xác nhận?}
    
    CheckEmpty -->|Không| ShowEmpty[Hiển thị:<br/>Không có đơn mượn nào<br/>chờ xác nhận]
    CheckEmpty -->|Có| SelectBorrow[Chọn đơn mượn<br/>để xử lý]
    
    ShowEmpty --> End
    
    SelectBorrow --> ShowActions[Hiển thị 2 lựa chọn:<br/>- Xác nhận<br/>- Từ chối]
    ShowActions --> UserAction{Người dùng<br/>chọn gì?}
    
    UserAction -->|Xác nhận| ProcessApprove[Luồng xác nhận]
    UserAction -->|Từ chối| ProcessReject[Luồng từ chối]
    UserAction -->|Hủy| Cancel[Quay lại danh sách]
    
    Cancel --> DisplayList
```

## Luồng xác nhận đơn mượn

```mermaid
flowchart TD
    Start([Bắt đầu]) --> ClickApprove[Click nút Xác nhận]
    ClickApprove --> CheckAvailable{Sách còn sẵn?<br/>available_quantity > 0}
    
    CheckAvailable -->|Không| ErrorUnavailable[Hiển thị lỗi:<br/>Sách hiện không còn sẵn.<br/>Không thể xác nhận]
    CheckAvailable -->|Có| UpdateStatus[Cập nhật trạng thái đơn mượn<br/>thành: Đã mượn]
    
    ErrorUnavailable --> End([Kết thúc])
    
    UpdateStatus --> DecreaseAvailable[Giảm số lượng sách có sẵn:<br/>available_quantity - 1]
    DecreaseAvailable --> IncreaseBorrowed[Tăng số lượng sách đang mượn:<br/>borrowed_quantity + 1]
    IncreaseBorrowed --> SaveDB[Lưu thay đổi vào database]
    SaveDB --> Success[Hiển thị thông báo:<br/>Xác nhận mượn sách thành công]
    Success --> RefreshList[Làm mới danh sách]
    RefreshList --> End
```

## Luồng từ chối đơn mượn

```mermaid
flowchart TD
    Start([Bắt đầu]) --> ClickReject[Click nút Từ chối]
    ClickReject --> ShowReasonForm[Hiển thị form nhập lý do từ chối]
    ShowReasonForm --> InputReason[Nhập lý do từ chối<br/>bắt buộc]
    InputReason --> ValidateReason{Kiểm tra lý do}
    
    ValidateReason -->|Lý do để trống| ErrorEmpty[Hiển thị lỗi:<br/>Vui lòng nhập lý do từ chối]
    ErrorEmpty --> InputReason
    
    ValidateReason -->|Lý do hợp lệ| UpdateStatus[Cập nhật trạng thái đơn mượn<br/>thành: Bị từ chối]
    UpdateStatus --> SaveReason[Lưu lý do từ chối vào database]
    SaveReason --> SaveDB[Lưu thay đổi vào database]
    SaveDB --> Success[Hiển thị thông báo:<br/>Đã từ chối đơn mượn]
    Success --> RefreshList[Làm mới danh sách]
    RefreshList --> End([Kết thúc])
```

## Luồng thay thế

### Luồng 1: Người dùng chưa đăng nhập
- Hệ thống chuyển hướng đến trang đăng nhập

### Luồng 2: Người dùng không phải nhân viên
- Hệ thống chuyển hướng đến dashboard phù hợp với vai trò

### Luồng 3: Không có đơn mượn chờ xác nhận
- Hiển thị thông báo "Không có đơn mượn nào chờ xác nhận"

### Luồng 4: Xác nhận nhưng sách không còn sẵn
- Hiển thị lỗi và không cho phép xác nhận
- Gợi ý kiểm tra lại số lượng sách

### Luồng 5: Hủy nhập lý do từ chối
- Người dùng có thể hủy và quay lại danh sách

## Xử lý lỗi

```mermaid
flowchart TD
    Error([Lỗi xảy ra]) --> CheckErrorType{Loại lỗi?}
    
    CheckErrorType -->|Sách không còn sẵn| ShowUnavailable[Hiển thị lỗi:<br/>Sách hiện không còn sẵn.<br/>Không thể xác nhận]
    CheckErrorType -->|Lý do từ chối để trống| ShowReasonError[Hiển thị lỗi:<br/>Vui lòng nhập lý do từ chối]
    CheckErrorType -->|Lỗi hệ thống| ShowSystemError[Hiển thị lỗi:<br/>Đã xảy ra lỗi hệ thống.<br/>Vui lòng thử lại sau]
    CheckErrorType -->|Lỗi database| ShowDBError[Hiển thị lỗi:<br/>Không thể cập nhật đơn mượn.<br/>Vui lòng thử lại]
    
    ShowUnavailable --> EndError([Kết thúc])
    ShowReasonError --> AllowRetry[Cho phép nhập lại lý do]
    ShowSystemError --> AllowRetry
    ShowDBError --> AllowRetry
    AllowRetry --> EndError
```

## Quy tắc validation

| Trường | Quy tắc |
|--------|---------|
| Lý do từ chối | Bắt buộc, không được để trống, tối đa 500 ký tự |

## Quy tắc nghiệp vụ

### Xác nhận đơn mượn:
1. Kiểm tra sách còn sẵn trước khi xác nhận
2. Cập nhật trạng thái: "Chờ xác nhận" → "Đã mượn"
3. Giảm `available_quantity` của sách đi 1
4. Tăng `borrowed_quantity` của sách lên 1

### Từ chối đơn mượn:
1. Bắt buộc phải nhập lý do từ chối
2. Cập nhật trạng thái: "Chờ xác nhận" → "Bị từ chối"
3. Lưu lý do từ chối để độc giả có thể xem (xem 2.3.3)
4. Không thay đổi số lượng sách

### Kiểm tra sách còn sẵn:
- Trước khi xác nhận, phải kiểm tra lại `available_quantity > 0`
- Có thể sách đã bị mượn bởi người khác trong lúc chờ xác nhận

## Chi tiết kỹ thuật

### Cập nhật số lượng sách khi xác nhận:
```sql
UPDATE books 
SET available_quantity = available_quantity - 1,
    borrowed_quantity = borrowed_quantity + 1
WHERE id = ? AND available_quantity > 0
```

### Cập nhật trạng thái đơn mượn:
```sql
UPDATE borrows 
SET status = 'Đã mượn',
    confirmed_at = NOW(),
    confirmed_by = ?
WHERE id = ? AND status = 'Chờ xác nhận'
```

### Từ chối đơn mượn:
```sql
UPDATE borrows 
SET status = 'Bị từ chối',
    rejection_reason = ?,
    rejected_at = NOW(),
    rejected_by = ?
WHERE id = ? AND status = 'Chờ xác nhận'
```

## Thông tin hiển thị trong danh sách

### Mỗi đơn mượn hiển thị:
- Tên độc giả
- Email độc giả
- Tên sách
- Tác giả
- Ngày yêu cầu mượn
- Thời hạn mượn (số ngày)
- Ngày hết hạn dự kiến
- Nút "Xác nhận" và "Từ chối"

