# 2.3.1 Mượn Sách (Độc Giả) - Borrow Book (Reader)

## Thông tin chung
- **Actor:** Độc giả
- **Yêu cầu:** Đăng nhập vào hệ thống với vai trò độc giả, đã xem chi tiết sách
- **Mô tả:** Cho phép độc giả tạo yêu cầu mượn sách

## Điều kiện tiên quyết

1. **Sách còn sẵn**: Số lượng sách có sẵn > 0
2. **Không vượt quá giới hạn**: Độc giả chưa mượn quá số sách tối đa (mặc định 5 cuốn)
3. **Không có khoản phạt chưa thanh toán**: Tất cả các khoản phạt phải đã được thanh toán

## Luồng chính

```mermaid
flowchart TD
    Start([Bắt đầu]) --> ViewBookDetails[Xem chi tiết sách]
    ViewBookDetails --> CheckLogin{Đã đăng nhập<br/>với vai trò độc giả?}
    
    CheckLogin -->|Không| RedirectLogin[Chuyển hướng đến<br/>trang đăng nhập]
    CheckLogin -->|Có| ClickBorrow[Click nút Mượn Sách]
    
    RedirectLogin --> End([Kết thúc])
    
    ClickBorrow --> CheckAvailable{Sách còn sẵn?<br/>số lượng > 0}
    
    CheckAvailable -->|Không| ErrorUnavailable[Hiển thị lỗi:<br/>Sách hiện không có sẵn]
    CheckAvailable -->|Có| CheckMaxBorrows{Độc giả đã mượn<br/>>= 5 cuốn?}
    
    ErrorUnavailable --> End
    
    CheckMaxBorrows -->|Có| ErrorMaxBorrows[Hiển thị lỗi:<br/>Bạn đã mượn tối đa 5 cuốn sách.<br/>Vui lòng trả sách trước]
    CheckMaxBorrows -->|Không| CheckUnpaidFines{Có khoản phạt<br/>chưa thanh toán?}
    
    ErrorMaxBorrows --> End
    
    CheckUnpaidFines -->|Có| ErrorUnpaidFines[Hiển thị lỗi:<br/>Bạn có khoản phạt chưa thanh toán.<br/>Vui lòng thanh toán trước]
    CheckUnpaidFines -->|Không| ShowBorrowForm[Hiển thị form mượn sách]
    
    ErrorUnpaidFines --> End
    
    ShowBorrowForm --> SelectDuration[Chọn thời hạn mượn:<br/>Mặc định: 14 ngày<br/>Tối đa: 30 ngày]
    SelectDuration --> ValidateDuration{Kiểm tra thời hạn}
    
    ValidateDuration -->|Thời hạn < 1 hoặc > 30 ngày| ErrorDuration[Hiển thị lỗi:<br/>Thời hạn mượn không hợp lệ<br/>1-30 ngày]
    ErrorDuration --> SelectDuration
    
    ValidateDuration -->|Hợp lệ| CreateBorrowRequest[Tạo đơn mượn<br/>Trạng thái: Chờ xác nhận]
    
    CreateBorrowRequest --> CalculateDueDate[Tính ngày hết hạn:<br/>Ngày hiện tại + thời hạn mượn]
    CalculateDueDate --> SaveDB[Lưu đơn mượn vào database]
    SaveDB --> Success[Hiển thị thông báo:<br/>Yêu cầu mượn sách đã được gửi.<br/>Vui lòng chờ nhân viên xác nhận]
    Success --> RedirectHistory[Chuyển hướng đến<br/>Lịch sử mượn sách]
    RedirectHistory --> End
```

## Luồng kiểm tra điều kiện chi tiết

```mermaid
flowchart TD
    Start([Bắt đầu]) --> CheckConditions[Kiểm tra điều kiện mượn sách]
    CheckConditions --> Check1{Sách còn sẵn?}
    
    Check1 -->|Không| Fail1[Lỗi: Sách không có sẵn]
    Check1 -->|Có| Check2{Độc giả đã mượn<br/>< 5 cuốn?}
    
    Check2 -->|Không| Fail2[Lỗi: Đã mượn tối đa]
    Check2 -->|Có| Check3{Không có phạt<br/>chưa thanh toán?}
    
    Check3 -->|Không| Fail3[Lỗi: Có phạt chưa thanh toán]
    Check3 -->|Có| Pass[Đủ điều kiện mượn sách]
    
    Fail1 --> End([Kết thúc])
    Fail2 --> End
    Fail3 --> End
    Pass --> End
```

## Luồng thay thế

### Luồng 1: Người dùng chưa đăng nhập
- Hệ thống chuyển hướng đến trang đăng nhập
- Sau khi đăng nhập, quay lại trang chi tiết sách

### Luồng 2: Sách không còn sẵn
- Hiển thị thông báo "Sách hiện không có sẵn"
- Vô hiệu hóa nút "Mượn sách"

### Luồng 3: Đã mượn tối đa số sách
- Hiển thị thông báo lỗi
- Gợi ý trả sách trước khi mượn sách mới

### Luồng 4: Có khoản phạt chưa thanh toán
- Hiển thị thông báo lỗi
- Cung cấp link đến trang thanh toán phạt

### Luồng 5: Hủy thao tác
- Người dùng có thể hủy và quay lại trang chi tiết sách

## Xử lý lỗi

```mermaid
flowchart TD
    Error([Lỗi xảy ra]) --> CheckErrorType{Loại lỗi?}
    
    CheckErrorType -->|Sách không có sẵn| ShowUnavailable[Hiển thị lỗi:<br/>Sách hiện không có sẵn]
    CheckErrorType -->|Đã mượn tối đa| ShowMaxBorrows[Hiển thị lỗi:<br/>Bạn đã mượn tối đa 5 cuốn sách]
    CheckErrorType -->|Có phạt chưa thanh toán| ShowUnpaidFines[Hiển thị lỗi:<br/>Bạn có khoản phạt chưa thanh toán]
    CheckErrorType -->|Thời hạn không hợp lệ| ShowDurationError[Hiển thị lỗi:<br/>Thời hạn mượn không hợp lệ]
    CheckErrorType -->|Lỗi hệ thống| ShowSystemError[Hiển thị lỗi:<br/>Đã xảy ra lỗi hệ thống.<br/>Vui lòng thử lại sau]
    
    ShowUnavailable --> EndError([Kết thúc])
    ShowMaxBorrows --> ShowReturnLink[Hiển thị link<br/>đến trang trả sách]
    ShowUnpaidFines --> ShowPaymentLink[Hiển thị link<br/>đến trang thanh toán phạt]
    ShowDurationError --> AllowRetry[Cho phép chọn lại thời hạn]
    ShowSystemError --> AllowRetry
    
    ShowReturnLink --> EndError
    ShowPaymentLink --> EndError
    AllowRetry --> EndError
```

## Quy tắc validation

| Trường | Quy tắc |
|--------|---------|
| Thời hạn mượn | Tối thiểu 1 ngày, tối đa 30 ngày, mặc định 14 ngày |

## Quy tắc nghiệp vụ

### Điều kiện mượn sách:
1. **Sách còn sẵn**: `available_quantity > 0`
2. **Không vượt quá giới hạn**: Số sách đang mượn (trạng thái "Đang mượn") < 5
3. **Không có phạt chưa thanh toán**: Tất cả phiếu phạt có `status != "Đã thanh toán"` phải = 0

### Thời hạn mượn:
- **Mặc định**: 14 ngày
- **Tối thiểu**: 1 ngày
- **Tối đa**: 30 ngày

### Trạng thái đơn mượn:
- Sau khi tạo: **"Chờ xác nhận"**
- Đợi nhân viên xác nhận (xem 2.3.2)

## Chi tiết kỹ thuật

### Dữ liệu đơn mượn:
- `book_id`: ID sách
- `reader_id`: ID độc giả
- `borrow_date`: Ngày mượn (ngày hiện tại)
- `due_date`: Ngày hết hạn (ngày hiện tại + thời hạn mượn)
- `status`: "Chờ xác nhận"
- `duration_days`: Số ngày mượn

### Tính ngày hết hạn:
```
due_date = current_date + duration_days
```

### Kiểm tra số sách đang mượn:
```sql
SELECT COUNT(*) 
FROM borrows 
WHERE reader_id = ? 
  AND status IN ('Chờ xác nhận', 'Đang mượn', 'Quá hạn')
```

### Kiểm tra phạt chưa thanh toán:
```sql
SELECT COUNT(*) 
FROM fines 
WHERE reader_id = ? 
  AND status != 'Đã thanh toán'
```

